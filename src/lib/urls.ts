const embedHosts = new Set(['www.youtube.com', 'www.youtube-nocookie.com', 'youtube.com', 'player.vimeo.com']);
export function safeExternalUrl(value?: string): string | undefined { if (!value) return undefined; try { const url = new URL(value); return ['https:', 'http:'].includes(url.protocol) ? url.toString() : undefined; } catch { return undefined; } }
export function safeEmbedUrl(value?: string): string | undefined { const url = safeExternalUrl(value); if (!url) return undefined; const parsed = new URL(url); if (embedHosts.has(parsed.hostname)) return url; if (parsed.hostname === 'youtu.be') return `https://www.youtube-nocookie.com/embed/${parsed.pathname.slice(1)}?rel=0`; if (parsed.hostname.endsWith('youtube.com') && parsed.pathname === '/watch') { const id = parsed.searchParams.get('v'); return id ? `https://www.youtube-nocookie.com/embed/${id}?rel=0` : undefined; } return undefined; }
